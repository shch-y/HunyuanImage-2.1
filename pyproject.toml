[build-system]
requires = ["setuptools<=73", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hunyuanimage-2-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "diffusers>=0.35.2",
    "einops>=0.8.1",
    "huggingface-hub[cli]>=0.36.0",
    "loguru>=0.7.3",
    "modelscope>=1.33.0",
    "numpy>=2.3.5",
    "omegaconf>=2.3.0",
    "pillow>=12.0.0",
    "qwen-vl-utils>=0.0.14",
    "safetensors>=0.7.0",
    "setuptools>=80.9.0",
    "torch>=2.6.0",
    "torchaudio>=2.6.0",
    "torchvision>=0.21.0",
    "tqdm>=4.67.1",
    "transformers[accelerate,tiktoken]>=4.57.3",
    "wheel>=0.45.1",
    "flash_attn",
]
[tool.setuptools]
packages = ["hyimage"]
